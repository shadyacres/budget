import { SelectivePreloadingStrategy } from './selective-preloading-strategy';

describe('SelectivePreloadingStrategy', () => {
  it('should create an instance', () => {
    expect(new SelectivePreloadingStrategy()).toBeTruthy();
  });
});
